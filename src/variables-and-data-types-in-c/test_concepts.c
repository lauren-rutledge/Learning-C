#include <stdbool.h>
#include <stdio.h>

main(void)
{
    bool testIntValues = 0;
    printf("%d\n", testIntValues); 
    // printf(testIntValues ? "true" : "false");
    // printf("%s", testIntValues ? "true" : "false");
    // fputs(testIntValues ? "true" : "false", stdout);
}